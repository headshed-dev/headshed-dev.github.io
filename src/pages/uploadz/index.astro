---
const title = "Uploadz";
const subtitle =
  "Articles and tutorials about web development, programming, and tech from Headshed";

import Layout from "../../layouts/Layout.astro";

import HeaderSmall from "../../components/HeaderSmall.astro";
import ProjectCard from "../../components/ProjectCards.astro";
import data from "../../data/site.json";

type Props = {
  projectTitle: string;
  href: string;
  body: string;
  type?: string;
  repo?: string;
};
---

<Layout title="Projects">
  <main>
    <HeaderSmall title={title} subtitle={subtitle} />
    <div class="">
      <section class="section">
        <div class="py-8 px-4 mx-auto max-w-screen-xl lg:py-16">
          <div class="grid md:grid-cols-2 sm:grid-cols-1 gap-8">
            {
              data.articles.map(
                (
                  value: {
                    
                    projectTitle: string;
                    href: string;
                    about: string;
                    repo?: string;
                    type?: string;
                    featuredImage: string;
                  },
                  index: number
                ) => {
                  return (
                    <ProjectCard
                      projectTitle={value.projectTitle}
                      href={value.href}
                      body={value.about}
                      type={value.type}
                      repo={value.repo}
                      featuredImage={value.featuredImage}
                    />
                  );
                }
              )
            }
          </div>
        </div>
      </section>
    </div>
  </main>
</Layout>

<style></style>
